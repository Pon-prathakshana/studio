export type AdCopy = {
  headline: string;
  bodyText: string;
  suggestedVisuals: string;
};

export type AdData = {
  id: string;
  headline: string;
  bodyText: string;
  imageUrl: string;
  imageHint: string;
};
